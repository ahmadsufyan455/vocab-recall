name: vocabulary_recall
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.2
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  assets:
    - assets/call-back.png

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/call-back.png"
  adaptive_icon_background: "#43A047"
  adaptive_icon_foreground: "assets/call-back.png"
